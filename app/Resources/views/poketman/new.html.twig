{% extends 'layout.html.twig' %}

{% block content %}
    <h5>Poketman creation</h5>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('poketman_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
